on:
  workflow_dispatch:
    inputs:
      browser:
        type: choice
        description: Which browser to test
        required: true
        options:
          - remote
jobs:
  memos-automation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v3
      - name: Set envs variables
        run: |
          touch ./home/.docker.env
          echo "${{ secrets.ENV_FILE }}" > ./home/.docker.env
      - name: Check envs
        run: cat ./home/.docker.env
      - name: Build project
        run: docker-compose build
      - name: Services Up
        run: |
          docker-compose --env-file ./home/.docker.env up -d
      - name: Wait for services
        run: sh wait.sh
      - name: Run tests
        run: docker-compose exec -T pytest-memos pytest --alluredir=./report
      - name: Generate report
        run: docker-compose exec -T pytest-memos allure generate ./report -o ./html
